# FDE Engagement Starter
# Kept deliberately small. You are building a walking skeleton inside a customer
# environment, not assembling a platform. Add what you need and justify it in an ADR.

# Customer environment (the mock legacy system you are integrating against)
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0

# Data handling
pandas>=2.2.0

# Retrieval
rank-bm25>=0.2.2                  # lexical half of hybrid retrieval
scikit-learn>=1.5.0               # TF-IDF dense baseline, no GPU or API key needed

# LLM (optional - the scaffold and every test run without an API key)
langchain-google-genai>=4.0.0     # free tier at https://aistudio.google.com/apikey
python-dotenv>=1.0.0

# Config and validation
pydantic>=2.0.0
pyyaml>=6.0.0

# Deployment (JWT verification for the in-VPC deploy gate)
pyjwt>=2.9.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
