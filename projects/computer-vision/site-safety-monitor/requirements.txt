# Everything the demo, the sweep, the gate and the tests need.
# Pinned rather than floored, so a reader's run matches the README's numbers.
numpy==1.26.4
pydantic==2.9.2
pydantic-settings==2.5.2
PyYAML==6.0.2
pytest==8.3.3
