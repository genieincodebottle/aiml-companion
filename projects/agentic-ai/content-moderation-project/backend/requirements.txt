# Everything needed to run the API, the agents and the tests.
#
# The ML toxicity classifier is NOT here. transformers and torch are imported
# lazily inside ml_classifier.py and only when USE_ML_MODELS=true, which is
# false by default, so installing them up front downloaded a large dependency
# that the default configuration never loads. They now live in
# requirements-ml.txt.
#
#     pip install -r requirements.txt        # run everything
#     pip install -r requirements-ml.txt     # add HateBERT/DistilBERT detection

# Core dependencies
fastapi==0.123.8
uvicorn[standard]==0.38.0
pydantic==2.12.5
python-multipart==0.0.20

# AI/ML frameworks
langchain-google-genai>=4.4.0
langgraph>=1.2.11

# Vector database
chromadb==1.3.5

# Security
bcrypt>=4.0.0

# Utilities
python-dotenv==1.2.1

# Testing
pytest>=8.0.0
requests>=2.32.0

# Observability and monitoring (optional but recommended)
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-instrumentation-fastapi>=0.51b0
